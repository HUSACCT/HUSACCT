using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace CSharpBenchmark.infrastructure.socialmedia.pinterest
{
    public class BoardDAO
    {
        public double boardCalculation()
        {
            return 2 * (1 - (Math.Pow(2, 4 - 1))) / (1 - 3);
        }
    }
}